# Program Name: Assignment2.py
# Course: IT3883/Section W01
# Student Name: Kyante
# Assignment Number: 2
# Due Date: 02/18/2026
# Purpose: 
# This program reads student data from an input file, calculates the 
# final average of six scores for each student, and prints the results 
# in descending order based on the final average.


file_name = input("Enter the input file name: ")

student_list = []

with open(file_name, "r") as input_file:
    
    for line in input_file:
        
        parts = line.strip().split()
        
        student_name = parts[0]
        
        scores = parts[1:]
        
        total = 0
        for score in scores:
            total += float(score)
        
        average = total / len(scores)
        
        student_list.append((student_name, average))


student_list.sort(key=lambda x: x[1], reverse=True)


print("\nFinal Averages (Highest to Lowest):")
for student in student_list:
    print(student[0], format(student[1], ".2f"))
